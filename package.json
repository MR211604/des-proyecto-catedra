{
  "name": "des-proyecto-catedra",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema ERP para un taller de costura",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "typecheck": "turbo run typecheck"
  },
  "packageManager": "pnpm@10.16.1",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "turbo": "^2.5.8"
  }
}
